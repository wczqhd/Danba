<view class="audio-player  bg-green round"   bind:tap="audioPlay" style="opacity: {{opcity}}">
	<text class="time">{{ time }}</text>
	<view
		class="controls play-btn"
	
		bind:tap="audioPlay">
		<image src=" ../../../../../images/voicemsgmy.png"></image>
	</view>
	<!-- <view
		class="controls pause-btn"
		wx:if="{{ curStatus == playStatus.PLAYING }}"
		bind:tap="audioPause">
		<image bind:tap="audioPause" src="../../../../../images/audioPause.png"></image>
	</view> -->
</view>
